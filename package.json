{
  "name": "kunafa-kingdom-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Main project for Kunafa Kingdom POS (frontend and backend)",
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev": "npm-run-all --parallel dev:frontend dev:backend",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "author": "Nagh (with AI Coder)",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
